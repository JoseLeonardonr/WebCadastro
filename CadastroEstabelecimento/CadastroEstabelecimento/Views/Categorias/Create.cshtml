@model CadastroEstabelecimento.Models.Categorias

@{
    ViewData["Title"] = "Nova Categoria";
}

<h2>@ViewData["Title"]</h2>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CategoriaDescricao" class="control-label"></label>
                <input asp-for="CategoriaDescricao" class="form-control" />
                <span asp-validation-for="CategoriaDescricao" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Cadastrar" class="btn btn-default" />
                <a asp-action="Index" class="btn btn-default btn-sm">Voltar</a>
            </div>
        </form>
    </div>
</div>



@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
